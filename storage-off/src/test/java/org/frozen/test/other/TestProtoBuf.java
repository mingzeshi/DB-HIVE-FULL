package org.frozen.test.other;

public class TestProtoBuf {
	
	public static void main(String[] args) {

		// 根据namespaceSource、aliasDSID（内码）、value的bytes，反序列化成protobuf对象：源数据对应的java对象
//		GeneratedMessage object = ProtoBufTools.makeProtobufObjectByBytes(namespaceSource, aliasDSID, bytes);
//		HashMap<String, List<GeneratedMessage>> map = ExtractRepository.doObjectExtract(newKey, object); // 提取对象
		
		
//		GeneratedMessage generatedMessage = ProtoBufTools.makeProtobufObjectByMap();
	}

}
